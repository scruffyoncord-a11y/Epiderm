# Epiderm landing page

This directory contains the standalone Epiderm marketing website.

It is a responsive single-page HTML site using Tailwind CSS through the CDN. No build step is required.

To preview it locally from the repository root:

```sh
python -m http.server 4173 --directory website
```

Then open `http://127.0.0.1:4173`.
